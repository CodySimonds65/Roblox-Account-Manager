<UserControl x:Class="RobloxAltClient.ClientsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RobloxAltClient"
             Loaded="ClientsPanel_Loaded">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox x:Name="ClientTabs" Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                 ScrollViewer.CanContentScroll="True"
                 ScrollViewer.PanningMode="HorizontalOnly"
                 Margin="0,0,0,2"
                 SelectionChanged="ClientTabs_SelectionChanged">
            <ListBox.Resources>
                <!-- Keep an overflowing tab strip usable without compressing
                     every client label into an unreadable sliver. The app
                     scrollbar template supplies the same dark track and violet
                     thumb as the rest of RAM. -->
                <Style TargetType="ScrollBar" BasedOn="{StaticResource {x:Type ScrollBar}}">
                    <Setter Property="Height" Value="14" />
                    <Style.Triggers>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter Property="Height" Value="Auto" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,6,0" />
                    <Setter Property="Padding" Value="14,7" />
                    <Setter Property="Background" Value="{StaticResource ElevatedBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Tab" Focusable="False"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Tab" Property="Background" Value="{StaticResource HoverBrush}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Tab" Property="Background" Value="{StaticResource AccentBrush}" />
                                        <Setter TargetName="Tab" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Label}" TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" MaxWidth="180" />
                        <Button Content="×" Width="20" Height="20" Margin="8,-2,-4,-2"
                                Padding="0" Background="Transparent" BorderThickness="0"
                                Foreground="{StaticResource MutedTextBrush}"
                                ToolTip="Close this Roblox client"
                                AutomationProperties.Name="Close Roblox client"
                                Click="CloseClientTab_Click" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="1" x:Name="HostArea" Background="#101318" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="12" Margin="0,8,0,0" ClipToBounds="True"
                SizeChanged="HostArea_SizeChanged">
            <local:EmbeddedClientHost x:Name="NativeClientHost" Focusable="True" />
        </Border>

        <TextBlock Grid.Row="2" x:Name="FocusDiagnostics" FontSize="10" Foreground="{StaticResource MutedTextBrush}"
                   MaxHeight="40" TextWrapping="Wrap" Margin="0,6,0,0" Visibility="Collapsed" />
    </Grid>
</UserControl>
